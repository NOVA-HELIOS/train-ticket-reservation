apiVersion: v1
kind: Service

metadata:
  name: train-ticket-service
  namespace: train-ticket

spec:
  selector:
    app: train-ticket

  type: LoadBalancer

  ports:
  - port: 80
    targetPort: 8080
